{
  "name": "clockart",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "workspaces": [
    "clock-app"
  ],
  "scripts": {
    "dev": "npm run dev -w clock-app",
    "build": "npm run build -w clock-app",
    "preview": "npm run preview -w clock-app",
    "deploy": "npm run deploy -w clock-app"
  }
}
